fullnameOverride: notify-db

annotations:
  sidecar.istio.io/inject: "false"

image:
  repository: pgvector
  name: pgvector
  tag: pg16
  pullPolicy: IfNotPresent

# Required for Debezium CDC to capture notification events
command: [ "postgres", "-c", "wal_level=logical" ]

env:
  POSTGRES_DB: notification-db
  POSTGRES_USER: ${POSTGRES_USR}
  POSTGRES_PASSWORD: ${POSTGRES_PWD}

persistence:
  enabled: true
  mountPath: /docker-entrypoint-initdb.d
  # This corresponds to ./database/notify-init

ports:
  - containerPort: 5432

service:
  ports:
    - name: notify-db
      port: 5432

resources:
  limits:
    memory: 512Mi