apiVersion: v2
appVersion: "1.0"
description: A Job to register Debezium connectors once Kafka Connect is ready
name: connector-init
version: 1.0.0

dependencies:
  - name: common
    version: 1.0.0
    repository: file://../../common